## Local development

### Setup

Requirements: python, pip, and virtualenv

Install virtualenv:
```sh
pip install virtualenv
```

Set up the virtualenv 
```sh
virtualenv venv
```

Activate the virtualenv & install the app's dependencies:
```sh
source venv/bin/activate
pip install -r requirements.txt
```

